{
  "name": "ai-payment-platform",
  "private": true,
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "test": "bun test",
    "typecheck": "tsc --build",
    "server": "bun run packages/server/src/server.ts",
    "playground": "bun run scripts/playground.ts",
    "demo": "bun --filter demo-app dev"
  },
  "devDependencies": {
    "@types/bun": "^1.4.0",
    "bun-types": "^1.4.0",
    "hono": "^4.13.5"
  }
}
