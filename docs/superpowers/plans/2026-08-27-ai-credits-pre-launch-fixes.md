# AI Credits Pre-Launch Fixes Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Close the pre-publication gaps found in the rebrand final review: rename the two public API identifiers that would be breaking after first npm publish (`AIPaymentWidget`/`<ai-payment-widget>` and `AIPaymentModal`), remove the fictional MCP guidance from docs, and rename the deploy identifiers to `ai-credits-gateway`.

**Architecture:** Two reviewable tasks. Task 1 renames the public SDK/React identifiers (custom-element tag + class + React component + file) across code, demo, docs and landing, keeping every usage in sync. Task 2 removes the non-runnable MCP tab content (no `/v1/mcp` route or CLI exists), renames deploy identifiers (`ai-payment-gateway` → `ai-credits-gateway` in `fly.toml` + `docs/DEPLOYMENT.md`), and polishes the last copy nit (`AI Credits Docs`). Both tasks are identifier/copy changes — zero behavior change.

**Tech Stack:** Bun 1.3, TypeScript, Hono, React, Web Components (custom element), Fly.io.

## Global Constraints

- `bun test` stays 263 pass / 9 skip / 0 fail; `bun run typecheck` exits 0. Zero behavior change — renames/copy only.
- Do NOT modify anything under `docs/superpowers/*` (historical).
- Do NOT change env var names, API routes (`/v1/*`), SQL migrations, demo keys (`pk_live_demo123`), or app package names.
- **Encoding:** use UTF-8-safe file writes on Windows PowerShell (never `Set-Content -Encoding UTF8`, which corrupts non-ASCII and adds BOMs). Pattern:
  ```powershell
  $enc = New-Object System.Text.UTF8Encoding($false)
  $c = [System.IO.File]::ReadAllText($path, [System.Text.Encoding]::UTF8)
  $c = $c -replace 'OLD', 'NEW'
  [System.IO.File]::WriteAllText($path, $c, $enc)
  ```
- Working tree clean after each commit. `bun.lock` is NOT hand-edited (regenerated by `bun install` if needed; the stale root label is inert and out of scope).

---

### Task 1: Rename public API identifiers to `AICreditsWidget` / `AICreditsModal`

**Files:**
- Modify: `packages/sdk/src/ui/widget.ts` (`AIPaymentWidget` class → `AICreditsWidget`; `customElements.define("ai-payment-widget", ...)` → `"ai-credits-widget"`; the `customElements.get(...)` guard likewise)
- Modify: `packages/sdk/tests/widget.test.ts` (imports and usages of `AIPaymentWidget`)
- Modify: `apps/demo/index.html` (`<ai-payment-widget ...></ai-payment-widget>` → `<ai-credits-widget ...>`; the `id="paymentWidget"` attribute and all JS `getElementById` references stay unchanged)
- Modify: `apps/docs/src/components/ai/TryInModal.tsx` (the `<ai-payment-widget ...>` snippet in the generated code)
- Modify: `apps/docs/src/components/tools/ErrorCodeTable.tsx` (recovery copy mentioning `<ai-payment-widget>`)
- Modify: `apps/docs/src/content/getting-started.tsx`, `apps/docs/src/content/sdk.tsx` (all `<ai-payment-widget>` code samples and prose)
- Modify: `apps/landing/src/components/ComponentStudio.tsx` (web-component snippets and `querySelector('ai-payment-widget')`)
- Modify: `apps/landing/src/components/HowItWorksSection.tsx` (the embedded widget tags)
- Rename: `packages/react/src/AIPaymentModal.tsx` → `packages/react/src/AICreditsModal.tsx`
- Modify: `packages/react/src/AICreditsModal.tsx` (interface `AIPaymentModalProps` → `AICreditsModalProps`; component `AIPaymentModal` → `AICreditsModal`)
- Modify: `packages/react/src/index.ts` (export path `./AIPaymentModal` → `./AICreditsModal`)
- Modify: `apps/landing/src/App.tsx` (import + `<AIPaymentModal ...>` usage → `AICreditsModal`)
- Modify: `apps/landing/src/components/ComponentStudio.tsx` (the two React-code snippets `import { AIPaymentModal } from "@ai-credits/react";` and `<AIPaymentModal ...>`)
- Test: existing suite (no new tests — behavior unchanged)

**Interfaces:**
- Consumes: inventory from the rebrand final review + verified file/line list.
- Produces: SDK exports `AICreditsWidget` and registers `<ai-credits-widget>`; React exports `AICreditsModal`; all docs/landing/demo usages in sync; `git grep 'ai-payment-widget|AIPaymentWidget|AIPaymentModal'` returns 0 matches outside `docs/superpowers/*`.

- [ ] **Step 1: Write the failing expectation**

Run: `git grep -n -E 'ai-payment-widget|AIPaymentWidget|AIPaymentModal' -- packages apps scripts tests docs ':!docs/superpowers'` (PowerShell: use `Select-String`; ripgrep may be unavailable).
Expected: 13 files listed (the inventory). This is the change list.

- [ ] **Step 2: Rename the SDK widget**

In `packages/sdk/src/ui/widget.ts`: `AIPaymentWidget` → `AICreditsWidget` (class declaration + every reference), `"ai-payment-widget"` → `"ai-credits-widget"` (both `customElements.get` and `customElements.define`). In `packages/sdk/tests/widget.test.ts`: `AIPaymentWidget` → `AICreditsWidget` (import + `new AICreditsWidget()` + the `SdkIndex.AICreditsWidget` assertion).

- [ ] **Step 3: Rename the React modal**

`git mv packages/react/src/AIPaymentModal.tsx packages/react/src/AICreditsModal.tsx`; then in the moved file replace `AIPaymentModal` → `AICreditsModal` (interface `AICreditsModalProps`, component name); in `packages/react/src/index.ts` replace `./AIPaymentModal` → `./AICreditsModal`.

- [ ] **Step 4: Update demo, docs and landing usages**

Replace `ai-payment-widget` → `ai-credits-widget` in `apps/demo/index.html`, `apps/docs/src/components/ai/TryInModal.tsx`, `apps/docs/src/components/tools/ErrorCodeTable.tsx`, `apps/docs/src/content/getting-started.tsx`, `apps/docs/src/content/sdk.tsx`, `apps/landing/src/components/ComponentStudio.tsx`, `apps/landing/src/components/HowItWorksSection.tsx`. Replace `AIPaymentModal` → `AICreditsModal` in `apps/landing/src/App.tsx` and both React snippets in `apps/landing/src/components/ComponentStudio.tsx`. Use the UTF-8-safe write pattern from Global Constraints. Do NOT change element `id` attributes, `project=` attribute values, or any runtime behavior.

- [ ] **Step 5: Verify**

Run: `git grep -n -E 'ai-payment-widget|AIPaymentWidget|AIPaymentModal' -- packages apps scripts tests docs ':!docs/superpowers'` → Expected: 0 matches (only `docs/superpowers/*` may retain them).
Run: `bun test` → 263 pass / 9 skip / 0 fail. Run: `bun run typecheck` → exit 0.
Encoding check on changed files: 0 BOM / 0 U+FFFD / no mojibake.

- [ ] **Step 6: Commit**

```bash
git add -A
git commit -m "feat(api): rename public identifiers to AICreditsWidget and AICreditsModal"
```

---

### Task 2: Remove MCP fiction, rename deploy identifiers, copy polish

**Files:**
- Modify: `apps/docs/src/components/ai/TryInModal.tsx` (MCP tab: replace non-runnable guidance with a „coming soon" state)
- Modify: `fly.toml` (`app = "ai-payment-gateway"` → `"ai-credits-gateway"`)
- Modify: `docs/DEPLOYMENT.md` (docker tag `ai-payment-gateway`, Fly app name, `https://ai-payment-gateway.fly.dev/`, `VITE_GATEWAY_URL=https://ai-payment-gateway.fly.dev` → `ai-credits-gateway`)
- Modify: `apps/docs/src/components/layout/DocsHeader.tsx` (`AI Credits Docs` → `AI Credits`)
- Test: existing suite

**Interfaces:**
- Consumes: Task 1's renamed identifiers; the final-review finding about the MCP tab.
- Produces: docs MCP tab that does not instruct users to run non-existent commands; deploy identifiers branded `ai-credits-gateway`; consistent header copy.

- [ ] **Step 1: Neutralize the MCP tab**

In `apps/docs/src/components/ai/TryInModal.tsx`, the MCP config section currently shows `claude mcp add ai-payment-gateway http://localhost:3000/v1/mcp` and a `bunx @ai-credits/server --project ...` command, and the provider map contains an `"ai-payment"` key (line ~65). The gateway has NO `/v1/mcp` route and the server package has NO CLI — these commands cannot succeed. Replace the tab's actionable content with a clearly-marked, non-actionable state: a single note such as „MCP server integration is coming soon." (keep the tab, remove the commands and the `"ai-payment"` provider key, or mark the section disabled — your judgment, but no user-facing text may instruct running a non-existent command). If the tab becomes empty after removal, replace its content with the coming-soon note instead of deleting the tab.

- [ ] **Step 2: Rename deploy identifiers**

In `fly.toml`: `app = "ai-payment-gateway"` → `app = "ai-credits-gateway"`.
In `docs/DEPLOYMENT.md`: every `ai-payment-gateway` occurrence → `ai-credits-gateway` (docker `-t ai-payment-gateway` and `docker run ... ai-payment-gateway`, the Fly app name comment, `https://ai-payment-gateway.fly.dev/`, and `VITE_GATEWAY_URL=https://ai-payment-gateway.fly.dev bun run demo`). Commands and structure unchanged.

- [ ] **Step 3: Copy polish**

In `apps/docs/src/components/layout/DocsHeader.tsx`: `AI Credits Docs` → `AI Credits`.

- [ ] **Step 4: Verify**

Run: `git grep -n 'ai-payment-gateway\|AI Credits Docs' -- fly.toml docs apps ':!docs/superpowers'` → Expected: 0 matches.
Run: `bun test` → 263 pass / 9 skip / 0 fail. Run: `bun run typecheck` → exit 0.
Encoding check on changed files: 0 BOM / 0 U+FFFD / no mojibake.

- [ ] **Step 5: Commit**

```bash
git add -A
git commit -m "fix(docs): remove MCP fiction, rename deploy identifiers to ai-credits-gateway"
```

---

## Self-Review Notes

- **Spec coverage:** every pre-launch follow-up from the rebrand final review maps to a step — widget tag + React modal (Task 1), MCP fiction + deploy identifiers + header copy (Task 2). The inert `bun.lock` root label is explicitly out of scope (Global Constraints).
- **Placeholder scan:** all replacements have exact source→target strings; the only judgment calls (MCP tab presentation) are bounded by an explicit rule (never instruct running a non-existent command).
- **Type consistency:** renames are mechanical; `export *` in `ui/index.ts` and `react/index.ts` picks up the new names without interface changes; typecheck is the completeness gate.
